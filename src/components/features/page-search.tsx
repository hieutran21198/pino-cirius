import styled from "@emotion/styled";
import { Box } from "components/shared/box";

const S = {
  PageSearch: styled(Box)``,
};
export const PageSearch: React.FC = () => {
  return <S.PageSearch></S.PageSearch>;
};
