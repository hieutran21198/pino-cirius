export enum RoutePath {
  // auth
  LOGIN = "/login",

  // public
  HOME = "/",
  SEARCH = "/search",
  POSTS = "/posts",
  TAGS = "/tags",
  ABOUT = "/about",

  OAUTH_REDIRECTED = "/oauth/redirect",
  // private
  CREATE_POST = "/management/posts/create",
}
